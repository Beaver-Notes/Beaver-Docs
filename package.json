{
  "scripts": {
    "build:css": "tailwindcss -i ./assets/main.css -o ./assets/main.compiled.css --minify",
    "jekyll": "npm run build:css && bundle exec jekyll build",
    "jekyll:serve": "npm run build:css && bundle exec jekyll serve"
  },
  "dependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "autoprefixer": "^10.4.20",
    "cssnano": "^7.0.6",
    "postcss": "^8.4.49",
    "simple-jekyll-search": "^1.10.0",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "postcss-cli": "^11.0.1"
  }
}
